# Core runtime dependencies for the 2D heat-equation PINO demo
torch>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
Pillow>=10.0.0
tqdm>=4.65.0

# Optional for result summaries and notebooks
pandas>=2.0.0
jupyter>=1.0.0
